require("zen-mode").setup {
    window = {
        width = 160,
    },
    plugins = {
        kitty = {
            enabled = false,
            font = "+2", -- font size increment
        },
    }
}
